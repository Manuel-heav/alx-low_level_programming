#include "main.h"
/**
 * print_sign - checks if number is negative or positive
 * @c: number to be checked
 *
 * Return: Always 0.
 */
int print_sign(int c)
{
if (c > 0)
return (1);
else if (c < 0)
return (-1);
else
return (0);
}
